# Textzy Mobile Android (WebView shell)

This app intentionally renders only the mobile UI from frontend:
- https://textzy.in/?mobileShell=1

It uses no custom native Compose UI to preserve the exact JSX design.
